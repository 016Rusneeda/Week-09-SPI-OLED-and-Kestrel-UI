# ใบงานการทดลองที่ 9.1
### การควบคุมจอแสดงผล OLED ผ่านบัส SPI และการออกแบบ Multi-Zone UI บน 1KB Framebuffer

> **คำชี้แจง:** ในใบงานนี้ นักศึกษาจะได้เรียนรู้การเชื่อมต่อและเขียนโปรแกรมควบคุมจอแสดงผล **OLED 0.96 นิ้ว ชิป SSD1306 แบบบัส SPI 4 สาย (7 ขา)** ด้วยเฟรมเวิร์ก ESP-IDF v6.x โดยมุ่งเน้นการทำความเข้าใจสถาปัตยกรรมระดับฮาร์ดแวร์ การส่งชุดคำสั่งเปิดวงจรทวีแรงดัน (Charge Pump) การบริหารจัดการหน่วยความจำภาพขนาด 1 KByte ด้วยคณิตศาสตร์ระดับบิต (Bitwise Manipulation) และการจัดแบ่งสัดส่วนหน้าจอ (Multi-Zone Layout) เพื่อเตรียมเชื่อมโยงกับ Kestrel Server ในใบงานถัดไป

---

## 1. วัตถุประสงค์การทดลอง (Objectives)
1. สามารถต่อวงจรจอแสดงผล OLED SSD1306 แบบ 4-wire SPI เข้ากับบัสฮาร์ดแวร์ SPI2 ของ ESP32 ได้อย่างถูกต้องและปลอดภัย
2. เข้าใจบทบาทหน้าที่ของขา **DC (Data/Command)**, ขา **RES (Reset)** และขา **CS (Chip Select)** ในการควบคุมคอนโทรลเลอร์หน้าจอ
3. สามารถเขียนโปรแกรมสั่งงาน **Hardware Reset** และส่งคำสั่ง **Magic Init Sequence** เพื่อเปิดการทำงานของวงจร Charge Pump ภายในชิปได้
4. เข้าใจโครงสร้างหน่วยความจำภาพขนาด **1,024 ไบต์ (8 Pages $\times$ 128 Columns)** และเขียนฟังก์ชันจุดพิกเซลด้วยคณิตศาสตร์ระดับบิต (Bitwise OR / AND-NOT)
5. สามารถเรนเดอร์ตัวอักษรผ่านตาราง **Font Matrix 5x7** และออกแบบหน้าจอแบ่งเป็น 3 โซน (**Multi-Zone UI: Header, Gauge Bar, Footer**) ได้

---

## 2. อุปกรณ์ที่ใช้ในการทดลอง (Hardware Components)
- บอร์ดไมโครคอนโทรลเลอร์ **ESP32** จำนวน 1 บอร์ด
- โมดูลจอแสดงผล **0.96" OLED SSD1306 (7-Pin SPI)** จำนวน 1 ชิ้น
- สายเชื่อมต่อ **USB Data Cable** จำนวน 1 เส้น
- แผงต่อวงจร (Breadboard) และสายไฟจัมเปอร์ (Jumper Wires) 7-10 เส้น

---

## 3. วงจรและการต่อสายฮาร์ดแวร์ (Schematic & Wiring)

```
        ESP32 Board                             OLED SSD1306 (7-Pin SPI)
      +--------------+                            +------------------+
      |          GND |----------------------------| GND              |
      |         3.3V |----------------------------| VCC              |
      |      GPIO 18 |----------------------------| D0 (SCK)         |
      |      GPIO 23 |----------------------------| D1 (MOSI)        |
      |       GPIO 4 |----------------------------| RES (Reset)      |
      |       GPIO 2 |----------------------------| DC (Data/Cmd)    |
      |       GPIO 5 |----------------------------| CS (Chip Select) |
      +--------------+                            +------------------+
```

### ⚠️ ข้อควรระวังด้านฮาร์ดแวร์
1. **ห้ามต่อขา VCC เข้ากับไฟ 5V (VIN) โดยเด็ดขาด** ให้ต่อเข้ากับพิน **3.3V** ของ ESP32 เพื่อป้องกันความเสียหายต่อชิป OLED
2. สังเกตชื่อขาบนแผงจอของตนเองให้ดี:
   - `D0` คือ **SPI Clock (SCK)**
   - `D1` คือ **SPI MOSI (Master Out Slave In)**
   - `RES` คือ **Reset**
   - `DC` คือ **Data / Command Select**

---

## 4. ขั้นตอนการทดลอง (Step-by-Step Activities)

> 📁 **ข้อกำหนดสำคัญด้านโฟลเดอร์โครงงาน (Project Isolation):**  
> ให้สร้างและเขียนโค้ดทั้งหมดไว้ในโฟลเดอร์ **`Week-09-SPI-OLED-and-Kestrel-UI/Example_codes/Lab9-1-SPI-OLED-MultiZone`**

---

### กิจกรรมที่ 1.1: ปลุกหน้าจอให้ติดด้วย Hardware Reset & Magic Init Sequence

ในกิจกรรมนี้ นักศึกษาจะได้เขียนฟังก์ชัน Reset ฮาร์ดแวร์ และส่งชุดคำสั่งเปิดหน้าจอ พร้อมทั้งถมหน้าจอด้วยลายตารางหมากรุก เพื่อพิสูจน์ว่าสายสัญญาณทั้งหมดเชื่อมต่ออย่างถูกต้อง

1. **การควบคุมขา Reset (RES Pin):**
   - ชิป SSD1306 ต้องการสัญญาณ Active-LOW เพื่อล้างสถานะวงจรตรรกะภายใน:
   ```c
   // ดึงขา RES ลง LOW ค้างไว้ 10 ms แล้วดึงกลับเป็น HIGH
   gpio_set_level(PIN_RES, 0);
   vTaskDelay(pdMS_TO_TICKS(10));
   gpio_set_level(PIN_RES, 1);
   vTaskDelay(pdMS_TO_TICKS(10));
   ```

2. **การแยกแยะคำสั่ง (Command) vs ข้อมูล (Data):**
   - เมื่อต้องการส่งคำสั่ง $\rightarrow$ ดึงขา **`DC = 0`**
   - เมื่อต้องการส่งข้อมูลพิกเซล $\rightarrow$ ดึงขา **`DC = 1`**

3. **คำสั่งเปิด Charge Pump (จุดตายสำคัญที่สุด):**
   - ต้องส่งคู่คำสั่ง `0x8D, 0x14` เพื่อเปิดการทำงานของวงจรทวีแรงดัน 7.5V ภายในชิป จากนั้นส่ง `0xAF` (Display ON)

---

### กิจกรรมที่ 1.2: การบริหารจัดการ 1KB Framebuffer & Bitwise Canvas

ชิป SSD1306 มีขนาด $128 \times 64$ พิกเซล แบ่งเป็น **8 Pages** ในแนวตั้ง (Page ละ 8 พิกเซล):

```
       Column 0                                                Column 127
Page 0 [Byte 0]   [Byte 1]   [Byte 2]  ... [Byte 126]  [Byte 127]  (Y = 0..7)
Page 1 [Byte 128] [Byte 129] ...       ...             [Byte 255]  (Y = 8..15)
...
Page 7 [Byte 896] ...                  ...             [Byte 1023] (Y = 56..63)
```

1. **การสร้าง Framebuffer ในแรมของ ESP32:**
   ```c
   static uint8_t s_oled_buffer[1024]; // 1 KByte พอดี
   ```

2. **สูตรการจุดพิกเซล (Bitwise Mapping):**
   เมื่อได้รับพิกัด $(x, y)$:
   - ดัชนีไบต์: `byte_index = x + (y / 8) * 128`
   - ตำแหน่งบิต: `bit_offset = y % 8`
   - จุดไฟ (สีขาว): `s_oled_buffer[byte_index] |= (1 << bit_offset);`
   - ดับไฟ (สีดำ): `s_oled_buffer[byte_index] &= ~(1 << bit_offset);`

3. **การส่งถ่ายข้อมูลขึ้นจอ (Flush via SPI):**
   - ส่งคำสั่งตั้งตำแหน่งเริ่มต้นที่ Column 0 และ Page 0
   - สลับขา **`DC = 1`**
   - ส่งข้อมูล 1,024 ไบต์จาก `s_oled_buffer` ผ่านบัส SPI ในคำสั่งเดียว

---

### กิจกรรมที่ 1.3: Font Matrix 5x7 & การแสดงผลสตริงตัวอักษร

ตัวอักษรแต่ละตัวในตาราง ASCII (เช่น `'A'`, `'B'`, `'1'`) สามารถแสดงได้ด้วยแมทริกซ์ขนาดความกว้าง 5 คอลัมน์ ความสูง 7 แถว (ใช้พื้นที่ 5 ไบต์ต่อตัวอักษร)

```c
// ตัวอย่างตัวอักษร 'A' (ความกว้าง 5 คอลัมน์)
// คอลัมน์ 0: 0x7E  (01111110)
// คอลัมน์ 1: 0x11  (00010001)
// คอลัมน์ 2: 0x11  (00010001)
// คอลัมน์ 3: 0x11  (00010001)
// คอลัมน์ 4: 0x7E  (01111110)
```

ฟังก์ชัน `oled_draw_char(int x, int y, char c)` จะนำข้อมูล 5 คอลัมน์นี้มาวนลูปจุดพิกเซลลงบน Framebuffer ช่วยให้เราสร้างฟังก์ชันพิมพ์ข้อความ `oled_draw_string(int x, int y, const char* str)` ได้อย่างอิสระ

---

### กิจกรรมที่ 1.4: Multi-Zone UI Layout (การจัดแบ่งพื้นที่จอ 3 โซน)

นำทักษะการวาดเส้น วาดกล่อง และพิมพ์ข้อความ มาจัดแบ่งพื้นที่หน้าจอขนาดเล็ก $128 \times 64$ ออกเป็น 3 ส่วนชัดเจน:

```
┌────────────────────────────────────────────────────────┐
│ ZONE 1: Header / Status Bar (Page 0-1 | Y: 0-15)       │
│ [Wi-Fi: OK]               [Kestrel: 5000]   [SYS: RUN] │
├────────────────────────────────────────────────────────┤
│ ZONE 2: Value & Gauge Bar   (Page 2-5 | Y: 16-47)      │
│  ADC RAW: 2048   (50.0%)                               │
│  [■■■■■■■■■■■■■■■■■■■■                    ]            │
├────────────────────────────────────────────────────────┤
│ ZONE 3: Message / Action Bar(Page 6-7 | Y: 48-63)      │
│  MSG: "Calibrated OK"                                  │
└────────────────────────────────────────────────────────┘
```

1. **Zone 1 (Header: Y: 0 - 15):** แสดงชื่อระบบหรือสถานะการเชื่อมต่อ และขีดเส้นใต้กั้นแบ่งโซน
2. **Zone 2 (Gauge Bar: Y: 16 - 47):** วาดกรอบสี่เหลี่ยม `draw_rect(10, 30, 108, 12)` และถมแถบความกว้างตามเปอร์เซ็นต์ `fill_rect(12, 32, bar_width, 8)`
3. **Zone 3 (Footer: Y: 48 - 63):** แสดงข้อความหรือค่าตัวแปรสตรีมมิ่ง

---

## 5. การทดสอบและรันโปรแกรม (Build & Flash)

รันคำสั่งคอมไพล์และแฟลชลงบน ESP32 ผ่าน Terminal:

```bash
# นำทางไปยังโฟลเดอร์โปรเจกต์
cd Week-09-SPI-OLED-and-Kestrel-UI/Example_codes/Lab9-1-SPI-OLED-MultiZone

# สั่งคอมไพล์และแฟลช
idf.py build flash monitor
```

*(หากใช้งานผ่าน Docker container ให้ใช้คำสั่ง `docker run` ตามสภาพแวดล้อมมาตรฐานของรายวิชา)*

---

## 6. แบบฝึกหัดท้าทายประจำใบงาน (Micro-Challenges)

> 🎯 **Challenge 9.1.1 (Anti-Plagiarism / แสดงเอกลักษณ์บุคคล):**  
> ปรับแต่งโค้ดใน **Zone 1 (Header Bar)** ให้แสดง **"รหัสนักศึกษา 4 ตัวท้าย"** เช่น `ID: 1234` ชิดขวาบนหน้าจอ พร้อมทั้งแสดงไอคอนรูปหัวใจหรือแบตเตอรี่แบบกำหนดเอง (Custom Bitmap Icon) ขนาด $8 \times 8$ พิกเซล

> 🎯 **Challenge 9.1.2 (Dynamic UI Animation):**  
> ปรับแต่งโค้ดใน **Zone 2 (Gauge Bar)** ให้แสดงผลเป็น **"แถบวัดค่าวัดสองทิศทาง (Center-Zero Meter)"** โดยให้จุดกึ่งกลางอยู่ที่ 0% (อยู่ตรงกลางจอที่ $X=64$) หากค่าติดลบให้แถบยื่นไปทางซ้าย หากค่าเป็นบวกให้แถบยื่นไปทางขวา

> 🎯 **Challenge 9.1.3 (Inverted Alert Animation):**  
> เขียนฟังก์ชันตรวจสอบ หากค่าเปอร์เซ็นต์ใน Zone 2 เกิน **90%** ให้สั่งคำสั่ง **`0xA7` (Invert Display)** เพื่อกระพริบหน้าจอเตือน (Alarm Mode) สลับกับ `0xA6` (Normal Display) ทุกๆ 200 มิลลิวินาที

---

## 7. คำถามท้ายการทดลองเพื่อการประเมินผล (Review Questions)

1. จงอธิบายความแตกต่างระหว่างการส่งข้อมูลผ่านขา DC เมื่อมีลอจิกเป็น `0` (LOW) และ `1` (HIGH) มาอย่างละเอียด
2. เหตุใดการต่อสายไฟ 3.3V และกราวด์ให้กับหน้าจอ SSD1306 จึงไม่ทำให้หน้าจอสว่างติดขึ้นมาในทันที? ต้องส่งชุดคำสั่งใดเป็นพิเศษเพื่อเปิดการทำงานของจอ?
3. ในหน่วยความจำภาพขนาด 1,024 ไบต์ หากต้องการเปลี่ยนสถานะของพิกเซลที่พิกัด $(x=75, y=35)$ ให้สว่างขึ้น จะต้องคำนวณตำแหน่ง Byte Index ในอาร์เรย์เป็นเท่าใด และต้องใช้ Bitmask ค่าใด?
4. สถาปัตยกรรมบัส SPI ให้ข้อได้เปรียบเหนือกว่าบัส I2C อย่างไรในการสร้างหน้าจอแสดงผล IoT แดชบอร์ดที่มีการอัปเดตข้อมูลความถี่สูง?
